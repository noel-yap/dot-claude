{
  "evals": [
    {
      "id": "mock-pain-class",
      "should_trigger": true,
      "file": "skills/dependency-injection/evals/samples/order_shipper.ts",
      "prompt": "I'm trying to test shipOrder in skills/dependency-injection/evals/samples/order_shipper.ts but every test ends up vi.mocking both ./db and ./email AND stubbing Date.now just to verify the international vs domestic subject line. Can you read the file and make it testable without monkey-patching those modules?",
      "expected_output": "Claude reads the file, recognises the DI smell (hardcoded module imports + wall clock inside the unit), and refactors shipOrder into a class or factory whose collaborators (store, mailer, clock) arrive as constructor parameters. The refactored SUT contains no bare references to db, emailService, Date.now, or console.; all I/O goes through this.X / deps.X. A composition root wires the production modules.",
      "assertions": [
        {
          "id": "introduces-injection-seam",
          "description": "Output introduces a constructor (or a function that takes a deps argument) that names the collaborators (store/db, mailer/email, clock) as parameters, separate from the original module imports."
        },
        {
          "id": "sut-has-no-bare-globals",
          "description": "The refactored SUT body contains no Date.now(, Math.random(, process.env, or console. — i.e., no bare global I/O reads inside the unit."
        },
        {
          "id": "sut-has-no-bare-module-refs",
          "description": "The refactored SUT body contains no bare db. or emailService. references; collaborators are reached via this.X or deps.X member access."
        },
        {
          "id": "preserves-region-rule",
          "description": "Refactor preserves the subject-line rule: international orders get 'Shipped (intl)', others get 'Shipped'."
        },
        {
          "id": "composition-root-present",
          "description": "Output includes a composition root (factory, main, or module-level const) that imports the real db/emailService/Date.now and wires them into the unit, separate from the unit's own definition."
        }
      ]
    },
    {
      "id": "hidden-time-and-env-fn",
      "should_trigger": true,
      "file": "skills/dependency-injection/evals/samples/token_issuer.ts",
      "prompt": "We can't unit-test issueToken in skills/dependency-injection/evals/samples/token_issuer.ts deterministically because Math.random, process.env.JWT_SECRET, and console.log are all baked into the function body. Please refactor so the test can pass deterministic values without poking globals.",
      "expected_output": "Claude refactors issueToken to take a deps parameter (or returns a curried factory) that supplies rng, secret, sign, and log. The function body uses deps.X calls; no Math.random, process.env, or console. appear inside it. A composition root supplies the production values.",
      "assertions": [
        {
          "id": "introduces-injection-seam",
          "description": "Output introduces a deps parameter (or factory) that names rng, secret, and log as required collaborators."
        },
        {
          "id": "sut-has-no-bare-globals",
          "description": "The refactored issueToken body contains no Math.random(, process.env, or console."
        },
        {
          "id": "no-production-default-deps",
          "description": "Injected deps are not given production defaults that silently fall back to Math.random / process.env / console — required by the signature or composed at the boundary."
        },
        {
          "id": "narrow-deps-interface",
          "description": "The deps type is narrow — it names only the operations actually used (rng, secret, sign, log) rather than re-exporting the whole jsonwebtoken or process surface."
        }
      ]
    },
    {
      "id": "decouple-from-global-singleton",
      "should_trigger": true,
      "file": "skills/dependency-injection/evals/samples/order_shipper.ts",
      "prompt": "Multiple handlers in our codebase reach into the global db singleton imported from ./db, and we want to swap one of them to a read-replica without touching every file. Can you decouple shipOrder in skills/dependency-injection/evals/samples/order_shipper.ts from the global db singleton so callers can pass in whichever store they want?",
      "expected_output": "Claude refactors shipOrder so the OrderStore arrives as a parameter (constructor or function argument), typed by a narrow interface. The unit no longer imports db directly. A composition root remains responsible for wiring the production db.",
      "assertions": [
        {
          "id": "introduces-injection-seam",
          "description": "Output introduces a parameter / constructor argument that supplies the store, replacing the direct ./db import inside the unit."
        },
        {
          "id": "sut-has-no-bare-module-refs",
          "description": "The refactored SUT body contains no bare db. references; the store is reached via this.X or deps.X."
        },
        {
          "id": "narrow-deps-interface",
          "description": "The store collaborator is typed by an interface naming only the operations the unit uses (getOrder, markShipped), not the full db module."
        },
        {
          "id": "composition-root-present",
          "description": "Output includes a composition root that constructs the unit with the production db, separate from the unit's own definition."
        }
      ]
    },
    {
      "id": "pure-function-no-di-needed",
      "should_trigger": false,
      "file": "skills/dependency-injection/evals/samples/pure_calculator.ts",
      "prompt": "Add a unit test for computeCartTotal in skills/dependency-injection/evals/samples/pure_calculator.ts that covers the empty-cart case, a single line with a percent discount, and a single line with a flat discount that exceeds the subtotal (it should clamp at zero, not go negative).",
      "expected_output": "Claude adds tests (or shows test code) that pass plain data to computeCartTotal and assert on the returned number. The function is NOT refactored to take a deps parameter; no DI is introduced because there is nothing to inject. The dependency-injection skill is NOT invoked, because the file is the canonical 'When NOT to use' case.",
      "assertions": [
        {
          "id": "adds-tests",
          "description": "Output contains test code that calls computeCartTotal directly with plain CartLine[] data and asserts on the result (e.g., a test or describe block, or example assertions)."
        },
        {
          "id": "no-deps-parameter-added",
          "description": "Output does NOT add a deps parameter, deps object, constructor injection, or any other DI seam to computeCartTotal / subtotal / applyDiscount / totalWithTax."
        },
        {
          "id": "no-collaborator-interfaces-introduced",
          "description": "Output does NOT introduce collaborator interfaces (e.g., Clock, Rng, Store, Mailer, TokenDeps) for the pure calculator — DI would be ceremony here."
        },
        {
          "id": "skill-not-invoked",
          "description": "The dependency-injection skill is not invoked during this run (e.g., no Skill tool use targeting it in the stream)."
        }
      ]
    }
  ]
}