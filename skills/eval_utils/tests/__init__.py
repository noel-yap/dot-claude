"""Unit tests for the skill-independent `eval_utils` package.

One test module per package submodule (`test_stream_json`, `test_runner`,
`test_text_utils`, `test_grading`, `test_plugin`). These cover the harness
logic once, so per-skill `evals/test_helpers.py` files only test their thin
SKILL_NAME-bound wrappers.
"""